"""Blue-identity card catalog. Empty for now -- no implemented deck plays
a blue card yet. Same shape as every other color file once populated:
a BLUE_CARD_CATALOG dict (name -> CardDef) and a BLUE_EFFECT_REGISTRY
dict (EffectId -> spec), unioned into game.CARD_DEFS/EFFECT_REGISTRY by
game/registry.py."""

BLUE_CARD_CATALOG = {}
BLUE_EFFECT_REGISTRY = {}

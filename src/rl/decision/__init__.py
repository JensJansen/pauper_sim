"""Turning an observation into a chosen action: the fixed/pointer action-table
bridge to drl_env (action_bridge), the per-seat decision dispatcher
(agent.SeatAgent) and its pregame-only stand-ins, and the gauntlet's
hand-authored, non-learned opponent (heuristic_agent)."""

"""Model package: per-card featurization + vocab (features), the shared
attention perception stack (arch), the per-deck actor-critic (deck), and the
per-deck mulligan model (mulligan). Training-loop and opponent-management
logic live in rl.training / rl.league, not here."""

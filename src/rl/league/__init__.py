"""Opponent pool + session orchestration: historical-opponent snapshots and
PFSP-weighted sampling (league.LeaguePool), and run_league.py's reusable
core -- _run_session, the eval-mode functions, and checkpoint/progress
bookkeeping (league_runner)."""

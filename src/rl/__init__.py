"""RL / self-play training harness: attention perception (arch), card
featurization + vocab (features), per-deck actor-critic (deck), the
fixed/pointer action bridge (action_bridge), the shared deck pool (pool),
the historical-opponent league (league), the PPO loop (train), and the
win/loss reward (rewards). Imports `game`/`drl_env` absolutely (src/ root)."""

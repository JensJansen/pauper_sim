{
  "_note": "Baseline mulligan-net bootstrap: opponent_mode=twin against the gauntlet twin's roster, always_keep pregame decider -- the settings train_mulligan.py itself defaults to when no config/flag overrides them. Checked in mainly to prove the --config path works end-to-end; a one-off ablation (--entropy-coef, --stratify-0land-pct, a narrower --decks, ...) is still meant to be passed as a flag override for that single invocation rather than edited in here, same convention as run_default.json's checkpoint_opponent_rate.",
  "opponent_mode": "twin",
  "league": "4_deck_subleague_test",
  "twin": "4_deck_subleague_gauntlet",
  "train_games": 1000,
  "update_every": 50,
  "eval_games": 100,
  "seed": 20260820,
  "twin_mulligan": "always_keep",
  "bootstrap_name": "mulligan_bootstrap"
}

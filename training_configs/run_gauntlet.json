{
  "_note": "The gauntlet population: an INDEPENDENTLY-trained twin of 4_deck_subleague_test (run_default.json) -- same roster, same run mechanics, same frozen shared stack, but its own checkpoint tree (league_name below) that never plays against the main league's live nets. The only intended difference from run_default.json is which checkpoint tree this points at; every other field is deliberately identical so any behavioral divergence measured later (rl.league_runner._run_eval_vs_gauntlet) comes from the independent training trajectory itself, not from different settings. See README's 'Gauntlet' section. Trained to a fixed depth (total_games below, ~8000 games/deck per the owner's 5-10k target) then left alone -- no code-level 'freeze', just stop invoking further sessions against this league_name once that depth is reached.",
  "snapshot_every_games": 200,
  "n_workers": 6,
  "checkpoint_opponent_rate": 0.5,
  "pfsp": true,
  "league_name": "4_deck_subleague_gauntlet",
  "roster": ["rakdos_madness", "dmir_terror", "elves", "mono_red_rally"],
  "total_games": 8000
}

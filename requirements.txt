# CUDA-enabled torch build (RTX 5060 Ti requires a CUDA 12.8+ wheel for
# Blackwell support). Pinned exactly so a plain `pip install -r
# requirements.txt` never silently swaps it back to a CPU-only build via
# stable-baselines3's torch>=2.8,<3.0 constraint.
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.11.0+cu128
gymnasium
stable-baselines3
sb3-contrib
